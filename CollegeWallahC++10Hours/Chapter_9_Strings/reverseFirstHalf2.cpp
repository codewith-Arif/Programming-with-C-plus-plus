#include<iostream>
#include<string>
using namespace std;
int main(){
    string s = "raghav";
    cout<<s<<endl;
    int n = s.length(); // n = 6
    // reverse(s.begin(),s.begin()+3);
    // reverse(s.begin(),s.begin()+n/2);
     cout<<s<<endl;
}